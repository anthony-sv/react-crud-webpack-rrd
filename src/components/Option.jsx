const Option = ({ image }) => {
    return (
        <>
            <div style={{ display: "block" }}>
                <img src={image} alt="" />
            </div>
        </>
    );
};

export default Option;
